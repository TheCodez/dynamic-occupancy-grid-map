get_filename_component(DOGM_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(DOGM_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@" "@CMAKE_CUDA_TOOLKIT_INCLUDE_DIRECTORIES@")

find_library(DOGM_LIBRARY NAMES dogm NO_DEFAULT_PATH HINTS ${DOGM_CMAKE_DIR}/../../lib/ REQUIRED)
#find_library(DOGM_SIMULATOR_LIBRARY NAMES dogm_simulator NO_DEFAULT_PATH HINTS ${DOGM_CMAKE_DIR}/../../lib/dogm/ REQUIRED)
#find_library(DOGM_UTILS_LIBRARY NAMES dogm_utils NO_DEFAULT_PATH HINTS ${DOGM_CMAKE_DIR}/../../lib/dogm/ REQUIRED)
find_package(CUDA REQUIRED)

SET(DOGM_LIBRARIES
    ${DOGM_LIBRARY}
#    ${DOGM_SIMULATOR_LIBRARY}
#    ${DOGM_UTILS_LIBRARY}
    ${CUDA_CUDART_LIBRARY}
)
